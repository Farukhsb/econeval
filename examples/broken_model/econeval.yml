project: broken-model
version: 1

invariants:
  - name: elasticity_must_be_negative
    expression: model.elasticity < 0

stress_tests:
  - name: stagflation_shock
    dataset: data/stagflation.csv
    metric: mape
    threshold: 0.01

fairness:
  enabled: false
  metrics: []

